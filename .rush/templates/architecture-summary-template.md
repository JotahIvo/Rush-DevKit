<!-- Keep this short on purpose: it is a pointer, not a mirror. Every skill that reads shared
     memory pays for this file's size on every read. -->
<!-- ARCHITECTURE-SUMMARY: the condensed digest of one spec's full architecture, appended to the
     shared .rush/memory/architecture.md so the project's long-term memory accumulates a summary
     per spec instead of the complete text of every one (that full text lives at
     specs/{{SPEC_ID}}/architecture.md and is never copied here). Written by /rush-architect right
     after it writes the full version. A later spec's architect reads this file to see the shape
     of decisions already made, then opens the linked full file only for the specs it actually
     needs detail from. -->

## {{SPEC_ID}} — {{SPEC_TITLE}}

**Full architecture**: `specs/{{SPEC_ID}}/architecture.md`

- **Shape**: {{ONE_LINE_SHAPE_SUMMARY}}
- **Key decisions**: {{DECISION_1}}; {{DECISION_2}}
- **New dependencies**: {{DEPENDENCIES_OR_NONE}}
- **Fitness functions**: {{FITNESS_FUNCTION_LIST_OR_NONE}}
- **Constraints this puts on future work**: {{CONSTRAINT_1}}
